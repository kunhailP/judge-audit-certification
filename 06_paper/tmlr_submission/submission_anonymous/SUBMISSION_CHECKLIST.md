# TMLR double-blind submission package (generated 2026-09-17)

Contents: main.tex (compiles standalone with the TMLR style), tmlr.sty, tmlr.bst, fancyhdr.sty, refs.bib, figures/ (the six figures the
paper uses), main.pdf (22 pages, `\usepackage{tmlr}` = anonymous review mode; the `[accepted]` option stays commented out).

Before uploading to OpenReview (https://openreview.net/group?id=TMLR):
1. Anonymity: the tex carries no author names, URLs, acknowledgements or repository links (checked). Script names such as
   `81_sampling_baselines.py` are mentioned; they do not identify the authors. If a code link is required at submission time,
   use an anonymised mirror (e.g. anonymous.4open.science) of the `featured-prep` branch, NOT the GitHub URL, and make sure the
   design log (01_design/) and lock notes do not contain names before mirroring.
2. Do not cite the repository's owner or the private Hugging Face dataset by name anywhere in the submission.
3. Fill the OpenReview form: TL;DR, abstract (copy from the PDF), primary area, "Claims / evidence" boxes; state that code and
   row-level results will be released on acceptance.
4. Broader-impact / reproducibility statement: the paper's Appendix "Reproducibility notes" and the lock record cover it.
5. Camera-ready later: uncomment `\usepackage[accepted]{tmlr}`, add authors, the repository URL and acknowledgements.
